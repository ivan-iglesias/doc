# Cambiar origen

Para cambiar el origen del repositorio ejecutar el siguiente comando

```
git remote set-url origin git@git.irontec.com:clientes/euskaltel/krt-landing-cro/euskaltel.git
```

Podemos comprobar que se ha cambiado correctamente con

```
git config --list
```